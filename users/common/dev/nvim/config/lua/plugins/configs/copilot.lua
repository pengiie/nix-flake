local options = {
  suggestion = {
    enabled = true,
    auto_trigger = true,
    debounce = 75,
    keymap = {
      accept = "<M-u>",
      accept_word = false,
      accept_line = false,
    },
  },
}

return options


