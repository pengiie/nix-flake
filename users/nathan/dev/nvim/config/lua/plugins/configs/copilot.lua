local options = {
  suggestion = {
    enabled = true,
    auto_trigger = false,
    debounce = 75,
    keymap = {
      accept = "<M-u>",
      next = "<M-]>",
      prev = "<M-[>",
      accept_word = false,
      accept_line = false,
    },
  },
}

return options


